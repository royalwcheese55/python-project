1. concurrency model
trhead vs process vs async i/o
workload type: cpu-bound vs i/o bound
memory behavior: object lifetime/ garbage collector overhead/ potential reference cycles
fault toleration: process boundaries and crash containment, resatrat and retry strategies

external dependencies; database latency and queue pressure.
observability: metrics/ tracing/ logging

threads: shared memory and limited by GIL for CPU work, good for I/O councurrency

processes: separate memory space and parallel CPU execution

async io: single thread eventloop and non blocking io.

practical consequences of gil
only one thread executes pyton bytecodes, limit cpu bound multithreading

heavy computation/ data processing/ ml workloads
determine is bottleneck: cpus usage low but threads high
profiling shows cpu contention

check if too many threads/ scheduler overhead/ lock contention/ workload mismatch

memroy growth without a leak: object accumulation cache/queues

misleading memory behavior: long-lived references, singletons, global variables

large object pools. background queues

source of concurrency issues: locking mistakes and blocking calls, gil contention, os layer and infrastructure.

diagnosing intermittent latency spikes
python interpreter: gc pauses, thread contention
app: slow code paths
i/o boundaries: db queries/ api calls
downstream service latency
infra: cpu spikes and network congestion

move code when python is too slow, cpu bound critical path
failure when scaling horizontally: state management, sessions/ in-memory caches
distributed consistency, race conditions

design large python codebases
maintainability: clear module boundaries and readable abstractions
correctness; strong tests/ type hints
decoupled components. safe deployments

failing architecture: tight coupling
cicular imports
framework lock-in

signs: small changes require large refactors/ modules tightly coupled
increasing bug frequency

good/bad abstractions
good reduce complexity, stable interface and hide implementation detail
bad: leaks behavior hard to debug and side effects

backward: semantic versioning
deprecation peroids and clear api contracts

risks: dynamic imports/ runtime attribute injection
poor observability, unpredictable behavior, harder debugging

good signals: fewer runtime errors and clearer contracts
bad: excessive type complexity

system pass test fails production
env differences/ imcomplete test coverage/ councurrency issues/ dependency changes

senior team focus on observability and reliability engineering, safe deployments