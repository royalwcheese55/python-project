## Installing Flask and Flask-SQLAlchemy with uv

To install Flask and Flask-SQLAlchemy, run in project directory:

```bash
uv pip install flask flask-sqlalchemy
```

You can verify the installation:

```bash
uv pip list
```

You should see `Flask` and `Flask-SQLAlchemy` in the list of installed packages. Now you're ready to build Flask apps!
